stages:
  - deploy

Deploy Docker:
  only:
    - master
  stage: deploy
  script:
    - docker-compose -f "dkc-prod.yml" down
    - docker-compose -f "dkc-prod.yml" up -d 
    - docker-compose -f "dkc-prod.yml" run yarn install
    - pwd